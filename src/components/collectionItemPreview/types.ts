import type { BindingCollectionField } from "../../types/email";

/** 数据预览行字段（与 payload itemFields 同型） */
export type CollectionPreviewField = BindingCollectionField;

export type CollectionItemPreviewMode = "readonly" | "editable";
