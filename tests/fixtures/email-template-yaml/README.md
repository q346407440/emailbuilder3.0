# 邮件模板 YAML 检查夹具

供 **`npm run template-yaml:golden`** / **`validate:all`** 使用：最小合法输入、期望 `template.json`、以及应报错的非法用例。

仓库**不设**业务用 YAML 收纳目录；本地调试展开：`npm run template-yaml:expand -- --in <路径> --out <输出.json>`。

格式与流程见 **`.cursor/skills/email-template-yaml-check/SKILL.md`**。
